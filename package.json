{
  "name": "ai-app-builder",
  "private": true,
  "workspaces": [
    "apps/studio",
    "packages/engine",
    "packages/templates",
    "packages/quality",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm --workspace apps/studio run dev",
    "build": "npm --workspaces run build",
    "lint": "npm --workspaces run lint",
    "typecheck": "npm --workspaces run typecheck"
  }
}
